
## 🔧 HOTFIX — Install Python deps & make run reliable

**Goal:** Fix `ModuleNotFoundError: No module named 'flask_sqlalchemy'` by adding requirements, installing them automatically on run, and restarting cleanly.

**Do:**

1. **Create/merge** a root `requirements.txt` (at repo root). If `api/requirements.txt` already exists, **merge it into the root file**. Use this list (pin light where safe):

```
Flask>=3.0
Flask-SQLAlchemy>=3.1
SQLAlchemy>=2.0
Flask-Migrate>=4.0
Flask-Bcrypt>=1.0
flask-jwt-extended>=4.6
Flask-Cors>=4.0
email-validator>=2.1
python-dotenv>=1.0
itsdangerous>=2.2
```

2. **Ensure Replit installs deps before running the app.**

* If the repo has a `.replit` file, update the `run` command to:

```
run = "pip install -q -r requirements.txt && python main.py"
```

* If there’s no `.replit`, **create one** with:

```
language = "python"
run = "pip install -q -r requirements.txt && python main.py"
```

3. **Verify import paths** still point to the right modules:

* `main.py` should import the app via:

  ```python
  from api.furrbutler import create_app
  app = create_app()
  if __name__ == "__main__":
      app.run(host="0.0.0.0", port=5000, debug=True)
  ```
* `api/furrbutler/__init__.py` must import `init_extensions` from `api.extensions` and call it inside `create_app()`.

4. **Run migrations (only if WO-0A created models):**

* Create a lightweight script `infra/scripts/manage.py` with commands `init-db`, `upgrade-db`.
* Execute:

  ```
  flask db upgrade
  ```

  (If no Flask CLI factory exists, skip migrations for now; the goal is to get the server running.)

5. **Restart** the app after dependency install.

**Acceptance Criteria:**

* Running the Replit project no longer throws `ModuleNotFoundError` for `flask_sqlalchemy` (or any listed deps).
* `python main.py` starts the server successfully (or the `.replit` run command does).
* Console shows Flask boot logs with no import errors.

**Do NOT** refactor JWT or frontend in this task—this is just dependency + run setup.